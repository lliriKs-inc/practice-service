# Выполненные исправления

## 2026-07-18 — Серверное хранение выбранной заявки студента

- **Кто?** Backend и Frontend.
- **Что исправлено?** Выбор рабочего трека среди нескольких одобренных заявок перенесён из localStorage в базу данных. Теперь он сохраняется для учётной записи, а не для конкретного браузера.
- **В каких файлах?** backend/prisma/schema.prisma, backend/prisma/migrations/20260718180000_add_active_application_to_user/migration.sql, backend/src/modules/auth/auth.service.ts, backend/src/modules/auth/auth.controller.ts, backend/src/modules/auth/auth.routes.ts, backend/src/modules/auth/dto/active-application.dto.ts, frontend/src/services/api/auth.ts, frontend/src/lib/api/session.ts, frontend/src/app/dashboard/applications/page.tsx, frontend/src/app/dashboard/tasks/page.tsx, frontend/src/app/dashboard/documents/page.tsx и их тесты. Удалён frontend/src/lib/active-application.ts.
- **Что именно изменено?** У User добавлено nullable-поле active_application_id с внешним ключом на Application и ON DELETE SET NULL. API PATCH /auth/me/active-application сохраняет выбор. Сервер проверяет, что заявка принадлежит текущему студенту, имеет статус APPROVED и что практика ещё не началась. GET /auth/me возвращает active_application_id. Все три страницы кабинета читают этот серверный выбор.
- **На что влияет?** «Задачи» и «Документы» открываются для одинакового трека в любом браузере и на любом устройстве того же студента. Выбор не является источником авторизации: каждый рабочий запрос по-прежнему проверяет владельца заявки на сервере. Если выбранная заявка отсутствует, интерфейс сохраняет безопасный fallback на первую одобренную заявку.
- **Что получилось?** Раньше выбор пропадал после очистки localStorage или при входе с другого устройства. Теперь он хранится в PostgreSQL, переживает смену браузера и обновление страницы. После начала практики и интерфейс, и API запрещают изменить выбранный трек.
- **Как протестировано?** Ручная проверка: студент выбрал трек в обычном браузере, затем вошёл в режиме инкогнито — «Задачи» и «Документы» показали тот же выбранный трек. Автоматически прошли Prisma validate/generate, backend typecheck и 3 теста AuthService; frontend typecheck и 15 тестов страниц заявок, задач и документов.

## 2026-07-18 — Замечание №8: скачивание решения тестового задания в Admin

- **Кто?** Backend и Frontend.
- **Что исправлено?** В карточке заявки администратора добавлена кнопка «Скачать решение», которая появляется только после загрузки решения студентом. Файл скачивается авторизованным запросом, поэтому доступ к нему сохраняется защищённым.
- **В каких файлах?** frontend/src/services/api/test-task.ts, frontend/src/app/admin/applications/page.tsx, frontend/src/app/admin/applications/page.test.tsx, frontend/src/lib/api/download.ts, backend/src/modules/test-task/test-task.service.ts, backend/src/shared/upload/filename.ts, backend/src/shared/upload/memory-upload.middleware.ts, backend/src/shared/upload/index.ts, backend/src/shared/upload/filename.test.ts.
- **Что именно изменено?** Frontend сохраняет download_path из API и передаёт его в общий механизм защищённого скачивания. API решения теперь возвращает исходное file_name. Добавлена нормализация multipart-имени файла: UTF-8 имя, ошибочно прочитанное загрузчиком как Latin-1, преобразуется обратно в корректную строку. Нормализация применяется как при новых загрузках, так и при отдаче сведений о ранее загруженном решении.
- **На что влияет?** Администратор может скачать решение без прямой публичной ссылки и с проверкой JWT. Русские и другие Unicode-символы в имени сохраняются и для будущих файлов, и для уже загруженных решений с повреждённой кодировкой. Решения без файла по-прежнему не показывают кнопку.
- **Что получилось?** Раньше Admin видел только факт загрузки, но не мог скачать файл. После добавления кнопки файл скачивался, однако имя из Content-Disposition становилось подчёркиваниями, а затем отображалось как mojibake (Ð…). Теперь скачивается исходный файл с исходным читаемым названием.
- **Как протестировано?** Ручная проверка: кнопка появляется у заявки с решением, файл успешно скачивается и имеет исходное русское имя «Индивидуальное задание на производственную практику для студента 3 курса.docx». Автоматически выполнены frontend typecheck, ESLint без ошибок и 7 тестов страницы заявок; backend typecheck и 23 теста upload/test-task модулей.

## 2026-07-18 — Замечание №7: ссылка приглашения работала только после сохранения

- **Кто?** Frontend.
- **Что исправлено?** Создание, перегенерация и удаление ссылки приглашения больше не являются локальными изменениями черновика когорты. Каждое действие сразу вызывает соответствующий API-метод и получает фактический токен, сохранённый сервером.
- **В каком файле?** frontend/src/app/admin/cohorts/page.tsx.
- **Что именно изменено?** Добавлены вызовы createInvitation, regenerateInvitation и deleteInvitation, состояние выполнения операции и обработка ошибок. После успешного ответа обновляются открытый черновик и список когорт. Кнопки блокируются на время запроса, а текст под полем явно сообщает, что ссылка уже готова к использованию.
- **На что влияет?** Ссылка становится валидной сразу после создания или перегенерации. При перегенерации прежний токен аннулируется сразу после успешного API-ответа. Удаление ссылки также применяется немедленно: закрытие модального окна без сохранения других вкладок не отменяет уже выполненную операцию со ссылкой.
- **Что получилось?** Раньше интерфейс временно показывал сгенерированный в браузере токен, которого сервер не знал; рабочая ссылка появлялась только после общего сохранения когорты и повторного открытия. Теперь в поле сразу отображается реальная рабочая ссылка.
- **Как протестировано?** Ручная проверка: новая ссылка открывает анкету без сохранения когорты; после перегенерации новая ссылка работает, старая становится невалидной; удалённая ссылка не открывается. Дополнительно успешно выполнены frontend typecheck и ESLint страницы когорт, а также backend InvitationService: 5 тестов пройдено.

## 2026-07-17 — Замечание №1: сессия на главной странице

- **Кто?** Frontend.
- **Что исправил?** Отображение состояния авторизации на главной странице после входа пользователя.
- **В каком файле?** `frontend/src/app/page.tsx`.
- **Что именно изменил?** Главная страница читает сохранённую сессию из `localStorage`: для авторизованного пользователя показывает кнопки «В кабинет» и «Выйти». Student направляется в `/dashboard`, Admin — сразу в `/admin/cohorts`; для гостя сохранены «Войти» и «Регистрация».
- **На что влияет?** Только на публичную главную страницу и маршрутизацию из неё. JWT, backend API и правила авторизации не изменялись.
- **Что получилось?** После входа и возврата на главную страница корректно отображает активную сессию вместо гостевого интерфейса. До исправления она всегда показывала «Войти» и «Регистрация», а Admin попадал на промежуточный маршрут с загрузочным экраном.
- **Как протестировано?** Ручная проверка под Student и Admin: вход, переход на `/`, обновление страницы, переход в кабинет и выход; оба сценария успешны. Дополнительно выполнены `npm.cmd run typecheck` и `npm.cmd run lint -- src/app/page.tsx` из каталога `frontend`, обе команды завершились без ошибок и предупреждений.

## 2026-07-17 — Замечание №2: дубли названий треков в когорте

- **Кто?** Frontend.
- **Что исправил?** Раннее обнаружение дублирующихся названий треков в редакторе когорты.
- **В каком файле?** `frontend/src/app/admin/cohorts/page.tsx`.
- **Что именно изменил?** Добавил проверку названий при создании, переименовании и сохранении треков. Сравнение не учитывает регистр и пробелы по краям; ошибка при переименовании отображается непосредственно под полем конкретной карточки, а при создании — под полем создания.
- **На что влияет?** Изменяет только UX редактора когорты. Серверная защита уникальным индексом `(cohort_id, title)` и обработка `409 TRACK_ALREADY_EXISTS` сохраняются как дополнительный барьер для конкурентных запросов и прямого API-доступа.
- **Что получилось?** Пользователь не может добавить визуальный дубликат в черновик и сразу понимает, какое поле нужно исправить. До изменения ошибка была доступна только после сохранения либо отображалась в общей верхней зоне, не связанной с переименовываемой карточкой.
- **Как протестировано?** Ручная проверка: создание `Backend`, попытки добавить `backend` и ` Backend `, переименование `Frontend` в `Backend`, исправление названия и успешное сохранение. Дополнительно выполнены `npm.cmd run typecheck` и `npm.cmd run lint -- src/app/admin/cohorts/page.tsx` из каталога `frontend`, обе команды завершились без ошибок и предупреждений.

## 2026-07-17 — Замечание №3: удаление когорты

- **Кто?** Frontend.
- **Что исправил?** Добавил удаление когорты из административного интерфейса.
- **В каком файле?** frontend/src/app/admin/cohorts/page.tsx, frontend/src/services/api/cohorts.ts, frontend/src/lib/api/error-messages.ts.
- **Что именно изменил?** Добавил клиентский вызов DELETE /cohorts/:id, кнопку удаления для черновика, собственную модалку подтверждения, отображение серверных ошибок и обновление списка после успешного удаления. В описании раздела явно указано, что удалить можно только когорту в статусе «Черновик».
- **На что влияет?** Интерфейс использует уже существующее серверное правило: ACTIVE/CLOSED когорты и когорты с заявками удалить нельзя. При удалении черновика сервер каскадно удаляет связанные настройки и очищает файлы тестовых заданий.
- **Что получилось?** Администратор может безопасно удалить тестовую черновую когорту через UI и видит последствия до подтверждения. До изменения такая возможность в интерфейсе отсутствовала; теперь нет системного браузерного подтверждения и карточки активных/закрытых когорт не перегружены неактивными кнопками.
- **Как протестировано?** Ручная проверка: отмена в модалке оставляет когорту, подтверждение удаляет тестовый черновик, ACTIVE/CLOSED когорты не показывают кнопку удаления. Дополнительно выполнены typecheck и ESLint-проверка страницы когорт без ошибок и предупреждений.

## 2026-07-17 — Замечание №4: копирование анкеты между когортами

- **Кто?** Frontend; backend-endpoint копирования уже существовал.
- **Что исправил?** Добавил в административный интерфейс копирование сохранённой анкеты в другую когорту.
- **В каком файле?** frontend/src/app/admin/cohorts/page.tsx, frontend/src/services/api/cohorts.ts.
- **Что именно изменил?** Подключил POST /surveys/:surveyId/copy, добавил кнопку и модалку выбора целевой когорты. Модалка выводится поверх редактора когорты; в список целей попадают только другие когорты без собственной анкеты.
- **На что влияет?** Копируется структура анкеты: название, вопросы, типы, обязательность, порядок и варианты ответов. Ответы кандидатов не копируются; существующая анкета целевой когорты не перезаписывается.
- **Что получилось?** Администратор может создать независимую копию анкеты без ручного повторного ввода вопросов. До изменения backend-возможность не была доступна из UI.
- **Как протестировано?** Ручная проверка: модалка открывается поверх редактора, выбор цели без анкеты создаёт копию, отмена ничего не меняет. Дополнительно выполнены frontend typecheck/ESLint и backend-тесты SurveyService: 6 тестов пройдены.

## 2026-07-18 — Экспериментальная логика заявок (замечания №5, №6 и №9)

- **Кто?** Backend и Frontend.
- **Статус решения.** Экспериментальное решение для текущего процесса практики. При необходимости его можно заменить на более строгий вариант ниже; точки отката перечислены отдельно.
- **Что исправлено?** Убрано ограничение, при котором активная заявка блокировала подачу на другие треки и когорты. Добавлены обработка PENDING-заявок при закрытии когорты, подтверждения действий администратора, причина отказа для студента и выбор рабочего трека среди одобренных заявок.
- **Файлы Backend.** backend/src/modules/application/application.service.ts, backend/src/modules/application/application.service.test.ts, backend/src/modules/cohort/cohort.service.ts, backend/src/modules/cohort/cohort.service.test.ts.
- **Файлы Frontend.** frontend/src/app/apply/[token]/page.tsx, frontend/src/app/apply/[token]/page.test.tsx, frontend/src/services/api/invitation.ts, frontend/src/lib/api/error-messages.ts, frontend/src/app/admin/applications/page.tsx, frontend/src/app/admin/applications/page.test.tsx, frontend/src/app/dashboard/applications/page.tsx, frontend/src/app/dashboard/applications/page.test.tsx, frontend/src/lib/active-application.ts, frontend/src/app/dashboard/tasks/page.tsx, frontend/src/app/dashboard/documents/page.tsx.
- **Логика подачи.** Студент может одновременно подать заявки на разные треки одной когорты и на треки других когорт. Заявки PENDING или APPROVED на других треках не блокируют новую заявку. Повторная заявка на тот же трек недоступна: это гарантирует уникальная пара пользователя и трека в базе данных.
- **Закрытие когорты.** При переводе активной когорты в CLOSED все её заявки PENDING автоматически становятся REJECTED с причиной «Когорта закрыта до рассмотрения заявки». Одобренные заявки не изменяются.
- **Решение администратора.** Администратор может одобрить несколько заявок одного студента на разные треки одной когорты. Другие заявки автоматически не отклоняются: окончательный выбор остаётся у студента. Перед одобрением и отклонением выводятся модальные окна в стиле сайта; при отклонении администратор может указать причину.
- **Действия студента.** Причина отклонения отображается в «Моих заявках». Среди одобренных заявок студент нажимает «Выбрать этот трек» и подтверждает действие в модальном окне. До даты начала практики выбор можно менять неограниченно; после начала кнопка блокируется и отображается «Выбор закреплён». После подтверждения студент остаётся на странице заявок.
- **Влияние выбора трека.** Выбор хранится локально в браузере под ключом active_application_id. Он определяет трек в разделах «Задачи» и «Документы» и показывается в их заголовках. Выбор не меняет серверные статусы и права: все одобренные заявки остаются одобренными.
- **Что было раньше?** Одна активная заявка блокировала новые, закрытие могло оставлять PENDING-заявки, студент не видел причину отказа, а подтверждение административных действий было неоднородным. Промежуточную логику, где одобрение одной заявки отклоняло остальные, отменили по результатам обсуждения.
- **Как протестировано?** Ручная проверка подтвердила: несколько заявок, автоматическое отклонение PENDING при закрытии, несколько одобренных заявок, оба модальных окна, причину отказа, выбор и смену трека до старта, блокировку после старта, отображение выбранного трека в задачах и документах, отсутствие перехода в задачи после выбора. Успешно выполнены backend typecheck и 20 профильных backend-тестов, frontend typecheck, ESLint и 20 профильных frontend-тестов.
- **Последствия и ограничения.** Выбор привязан к устройству и браузеру: после очистки localStorage или на другом устройстве будет выбран первый доступный одобренный трек. Для единого выбора на всех устройствах потребуется серверное поле и API. Вместимость треков текущей моделью не ограничивается.
- **Точки отката.** Для возврата к единственному одобрению на когорту нужно восстановить проверку и транзакционное отклонение остальных заявок в ApplicationService.updateStatus, а во Frontend убрать выбор трека. Для возврата блокировки подачи нужно вернуть проверку активных заявок в странице подачи и invitation API. Автоматическое отклонение PENDING при закрытии можно оставить или убрать независимо.

### Альтернативный строгий вариант — не реализован

- У студента только одна заявка в одну когорту, на выбранный трек; после REJECTED он может подать на другой трек; у треков есть ограниченные места.
- Он не выбран сейчас, потому что не соответствует текущей практике: сильный студент может быть одобрен на нескольких направлениях и сам выбрать итоговое.
- Для перехода потребуются миграция и уникальность по пользователю и когорте вместо пользователя и трека, логика повторной подачи после REJECTED, вместимость трека и транзакционная проверка мест при одобрении, изменения интерфейсов и пересмотр localStorage-выбора.
- Такой переход меняет бизнес-процесс и должен быть отдельной согласованной задачей с миграцией данных и повторной проверкой подачи, закрытия и одобрения.
